// var math = math || {};

// (function(){
//     //이 함수의 안쪽은 외부로부터 독립적인 스코프이다.
//     function sum(a,b){
//         return a + b;
//     }

//     math.sum = sum;//전역변수인 math에 sum이라는 함수를 할당한다.
// })();

export function sum(a,b){
    return a + b;
}